const createFieldDefinition = ({
  key,
  label,
  gridLabel = label,
  detailLabel = label,
  inputType = "text",
  templateWidth = 16,
  exportWidth = 16,
  sample = "",
}) =>
  Object.freeze({
    key,
    label,
    gridLabel,
    detailLabel,
    inputType,
    templateWidth,
    exportWidth,
    sample,
  });

const candidateFieldDefinitions = Object.freeze([
  createFieldDefinition({
    key: "designatedSort",
    label: "지정정렬",
    templateWidth: 12,
    exportWidth: 12,
    sample: "1",
  }),
  createFieldDefinition({
    key: "admissionYear",
    label: "모집년도",
    templateWidth: 12,
    exportWidth: 12,
    sample: "2026",
  }),
  createFieldDefinition({
    key: "track",
    label: "모집시기",
    templateWidth: 16,
    exportWidth: 14,
    sample: "수시",
  }),
  createFieldDefinition({
    key: "campus",
    label: "캠퍼스명",
    templateWidth: 16,
    exportWidth: 16,
    sample: "서울캠퍼스",
  }),
  createFieldDefinition({
    key: "campusCode",
    label: "캠퍼스코드",
    templateWidth: 14,
    exportWidth: 14,
    sample: "SEOUL",
  }),
  createFieldDefinition({
    key: "admission",
    label: "전형명",
    templateWidth: 18,
    exportWidth: 18,
    sample: "학생부종합전형",
  }),
  createFieldDefinition({
    key: "admissionCode",
    label: "전형코드",
    templateWidth: 16,
    exportWidth: 16,
    sample: "SU",
  }),
  createFieldDefinition({
    key: "series",
    label: "계열명",
    templateWidth: 16,
    exportWidth: 16,
    sample: "자연",
  }),
  createFieldDefinition({
    key: "seriesCode",
    label: "계열코드",
    templateWidth: 16,
    exportWidth: 16,
    sample: "NAT",
  }),
  createFieldDefinition({
    key: "unit",
    label: "모집단위명",
    templateWidth: 18,
    exportWidth: 18,
    sample: "간호학부",
  }),
  createFieldDefinition({
    key: "unitCode",
    label: "모집단위코드",
    templateWidth: 18,
    exportWidth: 18,
    sample: "NUR",
  }),
  createFieldDefinition({
    key: "major",
    label: "전공명",
    templateWidth: 16,
    exportWidth: 16,
    sample: "간호학전공",
  }),
  createFieldDefinition({
    key: "majorCode",
    label: "전공코드",
    templateWidth: 16,
    exportWidth: 16,
    sample: "NUR01",
  }),
  createFieldDefinition({
    key: "date",
    label: "시험날짜",
    inputType: "date",
    templateWidth: 16,
    exportWidth: 14,
    sample: "2026-03-28",
  }),
  createFieldDefinition({
    key: "time",
    label: "시작시간",
    inputType: "time",
    templateWidth: 12,
    exportWidth: 10,
    sample: "08:40",
  }),
  createFieldDefinition({
    key: "endTime",
    label: "종료시간",
    inputType: "time",
    templateWidth: 12,
    exportWidth: 10,
    sample: "10:00",
  }),
  createFieldDefinition({
    key: "period",
    label: "교시명",
    templateWidth: 16,
    exportWidth: 16,
    sample: "1교시",
  }),
  createFieldDefinition({
    key: "periodCode",
    label: "교시코드",
    templateWidth: 16,
    exportWidth: 16,
    sample: "P1",
  }),
  createFieldDefinition({
    key: "building",
    label: "고사건물명",
    templateWidth: 16,
    exportWidth: 16,
    sample: "본관",
  }),
  createFieldDefinition({
    key: "buildingCode",
    label: "고사건물코드",
    templateWidth: 16,
    exportWidth: 16,
    sample: "BLD01",
  }),
  createFieldDefinition({
    key: "room",
    label: "고사실명",
    templateWidth: 16,
    exportWidth: 16,
    sample: "101호",
  }),
  createFieldDefinition({
    key: "roomCode",
    label: "고사실코드",
    templateWidth: 16,
    exportWidth: 16,
    sample: "R101",
  }),
  createFieldDefinition({
    key: "examineeNo",
    label: "수험번호",
    templateWidth: 18,
    exportWidth: 16,
    sample: "173600001",
  }),
  createFieldDefinition({
    key: "temporaryNo",
    label: "가번호",
    templateWidth: 14,
    exportWidth: 14,
    sample: "A001",
  }),
  createFieldDefinition({
    key: "name",
    label: "이름",
    templateWidth: 16,
    exportWidth: 14,
    sample: "홍길동",
  }),
  createFieldDefinition({
    key: "birth",
    label: "생년월일",
    inputType: "date",
    templateWidth: 16,
    exportWidth: 14,
    sample: "2006-01-02",
  }),
  createFieldDefinition({
    key: "group",
    label: "조",
    templateWidth: 12,
    exportWidth: 12,
    sample: "A조",
  }),
  createFieldDefinition({
    key: "opt1",
    label: "OPT1",
    templateWidth: 14,
    exportWidth: 14,
    sample: "옵션1",
  }),
  createFieldDefinition({
    key: "opt2",
    label: "OPT2",
    templateWidth: 14,
    exportWidth: 14,
    sample: "옵션2",
  }),
  createFieldDefinition({
    key: "opt3",
    label: "OPT3",
    templateWidth: 14,
    exportWidth: 14,
    sample: "옵션3",
  }),
  createFieldDefinition({
    key: "opt4",
    label: "OPT4",
    templateWidth: 14,
    exportWidth: 14,
    sample: "옵션4",
  }),
  createFieldDefinition({
    key: "opt5",
    label: "OPT5",
    templateWidth: 14,
    exportWidth: 14,
    sample: "옵션5",
  }),
]);

const candidateFieldDefinitionMap = Object.freeze(
  candidateFieldDefinitions.reduce((definitionsByKey, definition) => {
    definitionsByKey[definition.key] = definition;
    return definitionsByKey;
  }, {}),
);

const candidateGridFieldKeys = Object.freeze([
  "designatedSort",
  "campus",
  "campusCode",
  "track",
  "admission",
  "admissionCode",
  "series",
  "seriesCode",
  "unit",
  "unitCode",
  "major",
  "majorCode",
  "date",
  "time",
  "endTime",
  "period",
  "periodCode",
  "building",
  "buildingCode",
  "room",
  "roomCode",
  "examineeNo",
  "temporaryNo",
  "name",
  "birth",
  "group",
  "opt1",
  "opt2",
  "opt3",
  "opt4",
  "opt5",
]);
const candidateDetailFieldKeys = Object.freeze([...candidateGridFieldKeys]);
const candidateWorkbookFieldKeys = Object.freeze([...candidateGridFieldKeys]);
const optionalTemplateFieldKeys = Object.freeze([
  "designatedSort",
  "endTime",
  "campusCode",
  "admissionCode",
  "seriesCode",
  "unitCode",
  "major",
  "majorCode",
  "periodCode",
  "buildingCode",
  "roomCode",
  "temporaryNo",
  "group",
  "opt1",
  "opt2",
  "opt3",
  "opt4",
  "opt5",
]);
const legacyTemplateHeaders = Object.freeze({
  admissionYear: "모집년도",
  admission: "전형",
  building: "고사건물",
  date: "시험일자",
  major: "전공",
  period: "교시",
  room: "고사실",
  series: "계열",
  time: Object.freeze(["시험시간", "시간"]),
  endTime: Object.freeze(["종료시간", "끝시간"]),
  track: "시험",
  unit: "모집단위",
});

const getFieldDefinition = (key) => candidateFieldDefinitionMap[String(key || "").trim()] || null;

module.exports = Object.freeze({
  candidateDetailFieldKeys,
  candidateFieldDefinitions,
  candidateGridFieldKeys,
  candidateWorkbookFieldKeys,
  getFieldDefinition,
  legacyTemplateHeaders,
  optionalTemplateFieldKeys,
});
